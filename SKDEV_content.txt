=== SK-CONTEXT MANIFEST ===
Intent: SKDEV
Date: 2026-09-19 14:56:37
AST Mode: False
Line Numbers: True
Modules: networkStyles.js, drawNodes.ts
Processed Files: 2
===========================



// --- frontend/src/views/analysis/network/networkStyles.js ---

[L001, S0] export const CLUSTER_METADATA = {
[L002, S4]     1: { name: 'Primary Collaboration Cluster', color: '#ff1a2e' },
[L003, S4]     2: { name: 'Core Scientific Cluster', color: '#1a5cff' },
[L004, S4]     3: { name: 'Emerging Research Cluster', color: '#00c853' },
[L005, S4]     4: { name: 'Secondary Hub', color: '#00b8d4' }
[L006, S0] };
[L007, S0] 
[L008, S0] /**
[L009, S1]  * Returns the hex color for a given node cluster group.
[L010, S1]  */
[L011, S0] export const getNodeColor = (group) => {
[L012, S4]     const cluster = CLUSTER_METADATA[group];
[L013, S4]     return cluster ? cluster.color : '#9ca3af';
[L014, S0] };
[L015, S0] 
[L016, S0] // Rango de radios general (en px de grafo, antes de aplicar `scale`).
[L017, S0] // Reducido respecto a la versión anterior (antes tope 30) para que la red
[L018, S0] // se vea menos "pesada" en general.
[L019, S0] export const RADIUS_RANGE = { min: 3, max: 13 };
[L020, S0] 
[L021, S0] const getMetric = (node) =>
[L022, S0] node.degree !== undefined ? node.degree : (node.citations || node.papers || 1);
[L023, S0] 
[L024, S0] /**
[L025, S1]  * Normaliza el radio de cada nodo en dos pasos:
[L026, S1]  *
[L027, S1]  * 1. Variación LOCAL: dentro de su propio grupo/color, el nodo más conectado
[L028, S1]  *    se ve más grande que el menos conectado de ESE MISMO grupo.
[L029, S1]  * 2. Techo GLOBAL: el tamaño máximo que puede alcanzar un grupo depende de
[L030, S1]  *    qué tan conectado está su nodo más importante frente al nodo más
[L031, S1]  *    conectado de TODA la red. Así, un clúster aislado y pequeño (ej. 4
[L032, S1]  *    autores en Vietnam) nunca compite en tamaño con los hubs del clúster
[L033, S1]  *    principal — pero sigue teniendo variación interna entre sus propios
[L034, S1]  *    nodos.
[L035, S1]  *
[L036, S1]  * Sin el paso 2, cada color llenaría el mismo rango de tamaños sin importar
[L037, S1]  * su importancia real, y un clúster de 3 personas se vería tan "grande" como
[L038, S1]  * el clúster de colaboración primario — perdiendo esa jerarquía visual.
[L039, S1]  *
[L040, S1]  * Debe llamarse una vez, después de calcular `node.degree`, y antes de usar
[L041, S1]  * `calculateRadius` para el layout (separación de islas, colisiones, etc.).
[L042, S1]  * Guarda el resultado en `node.radius`.
[L043, S1]  */
[L044, S0] export const assignNodeRadii = (nodes) => {
[L045, S4]     const globalMax = Math.max(...nodes.map(getMetric), 1);
[L046, S0] 
[L047, S4]     const byGroup = new Map();
[L048, S4]     nodes.forEach(n => {
[L049, S8]         const list = byGroup.get(n.group) || [];
[L050, S8]         list.push(n);
[L051, S8]         byGroup.set(n.group, list);
[L052, S4]     });
[L053, S0] 
[L054, S4]     byGroup.forEach(list => {
[L055, S8]         const metrics = list.map(getMetric);
[L056, S8]         const groupMin = Math.min(...metrics);
[L057, S8]         const groupMax = Math.max(...metrics);
[L058, S0] 
[L059, S8]         // Techo del grupo: proporcional a qué tan conectado está su nodo más
[L060, S8]         // importante frente al máximo global (nunca por debajo de RADIUS_RANGE.min).
[L061, S8]         const groupCeiling = RADIUS_RANGE.min +
[L062, S8]         (RADIUS_RANGE.max - RADIUS_RANGE.min) * (groupMax / globalMax);
[L063, S0] 
[L064, S8]         list.forEach(n => {
[L065, S12]             const metric = getMetric(n);
[L066, S12]             // Si todos los nodos del grupo tienen el mismo grado (o el grupo
[L067, S12]             // tiene un solo nodo), no hay variación interna que mostrar: van
[L068, S12]             // directo al techo de su grupo.
[L069, S12]             const t = groupMax > groupMin ? (metric - groupMin) / (groupMax - groupMin) : 1;
[L070, S12]             n.radius = RADIUS_RANGE.min + t * (groupCeiling - RADIUS_RANGE.min);
[L071, S8]         });
[L072, S4]     });
[L073, S0] };
[L074, S0] 
[L075, S0] /**
[L076, S1]  * Calcula el radio final de un nodo. Usa `node.radius` (calculado por
[L077, S1]  * `assignNodeRadii`) si ya está disponible; si no, cae a una fórmula
[L078, S1]  * genérica por si algún nodo llega sin pasar por ese paso.
[L079, S1]  */
[L080, S0] export const calculateRadius = (node, scale = 1) => {
[L081, S4]     if (!node) return RADIUS_RANGE.min * scale;
[L082, S4]     if (node.radius !== undefined) return node.radius * scale;
[L083, S4]     const value = Math.max(0, getMetric(node));
[L084, S4]     return Math.min(RADIUS_RANGE.max, RADIUS_RANGE.min + Math.sqrt(value) * 1.4) * scale;
[L085, S0] };


// --- frontend/src/graph/render/drawNodes.ts ---

[L001, S0] import type { NetworkNode } from "../types";
[L002, S0] 
[L003, S0] const CLUSTER_PALETTE = {
[L004, S4]     red: { start: "#ff6b6b", end: "#c92a2a" },
[L005, S4]     blue: { start: "#4dabf7", end: "#1864ab" },
[L006, S4]     green: { start: "#69db7c", end: "#2b8a3e" },
[L007, S0] };
[L008, S0] 
[L009, S0] /**
[L010, S1]  * Pure function to render spherical 3D nodes.
[L011, S1]  * Iterates context drawing for individual radial gradients.
[L012, S1]  */
[L013, S0] export const drawNodes = (
[L014, S4]     ctx: CanvasRenderingContext2D,
[L015, S4]     nodes: NetworkNode[]
[L016, S0] ) => {
[L017, S4]     nodes.forEach((node) => {
[L018, S8]         if (node.x == null || node.y == null) return;
[L019, S0] 
[L020, S8]         ctx.beginPath();
[L021, S8]         ctx.arc(node.x, node.y, node.radius, 0, 2 * Math.PI);
[L022, S0] 
[L023, S8]         // Offset gradient origin to simulate top-left directional lighting
[L024, S8]         const offset = node.radius / 3;
[L025, S8]         const grad = ctx.createRadialGradient(
[L026, S12]             node.x - offset, node.y - offset, 0,
[L027, S12]             node.x, node.y, node.radius
[L028, S8]         );
[L029, S0] 
[L030, S8]         const colors = CLUSTER_PALETTE[node.cluster as keyof typeof CLUSTER_PALETTE] || { start: "#9ca3af", end: "#4b5563" };
[L031, S8]         grad.addColorStop(0, "#ffffff");
[L032, S8]         grad.addColorStop(0.3, colors.start);
[L033, S8]         grad.addColorStop(1, "#1f2937");
[L034, S0] 
[L035, S8]         ctx.fillStyle = grad;
[L036, S8]         ctx.fill();
[L037, S8]         
[L038, S8]         ctx.lineWidth = 0.5;
[L039, S8]         ctx.strokeStyle = '#111827';
[L040, S8]         ctx.stroke();
[L041, S4]     });
[L042, S0] };
